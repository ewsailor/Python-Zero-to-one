x = input("請輸入一個正整數：") # 4

if x.isdigit():  
    n = int(x) 
    f = 1 
    sum = 0
    for i in range(1, n + 1):
        f *= i   # f = f * i，計算 i!
        sum += f # 累加到總和
    print(f"1! + 2! + ... + {n}! 的總和是：{sum}") 
else:
    print(f"{x} 並非正整數無法運算，請輸入正整數")
